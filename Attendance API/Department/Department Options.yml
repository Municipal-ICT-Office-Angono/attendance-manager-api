info:
  name: Department Options
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/hris/departments/options"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
